# LLM 学习项目完整计划

> 生成日期：2026-08-15
> 学习基线：已有机器学习基础
> 项目链路：**LLMs-from-scratch → nanochat → ai-engineering-from-scratch**
> 预估周期：8–12 周，每日 2–3h

---

## 一、最终执行架构：旧笔记本 + 远程 GPU 云服务器

### 1.1 硬件定位

| 角色 | 设备 | 用途 | 限制 |
|------|------|------|------|
| 本地客户端 | 旧笔记本 i7-4700HQ | SSH 终端、VSCode Remote-SSH、代码编辑、Notebook 前端浏览、文件同步 | 四代酷睿，内存大概率 8G/16G；**绝对不要本地跑 LLM 训练、7B 模型推理**，仅作为操作终端 |
| 远端计算 | 公有云 GPU 服务器 | 全部训练、微调、模型推理任务 | 拥有 CUDA，支持 FP8、DDP、大 batch，不受本地内存约束 |

### 1.2 整套工作流

1. 本地安装：Git、VSCode + **Remote - SSH 插件**、OpenSSH 客户端、WSL2（推荐，SSH 稳定性更好）、`rsync` / WinSCP
2. VSCode 通过 SSH 直连远端服务器，直接在远端环境写代码、运行 Notebook、启动训练
3. Jupyter Notebook 端口转发：
   ```bash
   # 本地终端执行，远端 8888 端口映射到本地 localhost:8888
   ssh -L 8888:127.0.0.1:8888 user@服务器IP
   ```
4. Tensorboard 端口转发同理 `-L 6006:127.0.0.1:6006`
5. 训练输出日志、模型权重按需同步回本地，不要实时同步大权重文件

### 1.3 远端必备工具

- PyTorch（CUDA）、uv、jupyter lab
- **tmux / screen**：断开 SSH 后训练进程持续后台运行（重中之重）

```bash
# tmux 最简用法
tmux new -s llm_train      # 创建会话
# 执行训练脚本
Ctrl+B 松开再按 D           # 后台脱离
tmux a -t llm_train         # 重新接入会话
```

### 1.4 i7-4700HQ 旧笔记本避坑清单

1. 不要尝试本地运行任何 LLM 训练；四代酷睿无 AVX2 优化，速度极差，极易内存爆掉
2. 无线网络不稳定建议网线直连；SSH 断连会直接终止进程，**必须 tmux**
3. Jupyter 远程访问优先端口转发，不要直接开放服务公网暴露，存在安全风险
4. 大文件（数据集、模型权重）使用 `rsync` 增量同步，避免重复传输

```bash
# 远端 → 本地拉取文件夹示例
rsync -avz user@ip:/path/to/logs ./local_logs
```

---

## 二、云服务提供商选型（德国/欧洲场景）

### 2.1 综合结论

**首选 RunPod 社区云 + RTX 3090 24GB 可中断实例**，兼顾性价比、易用性和稳定性。

### 2.2 三家平台详细对比

| 平台 | 欧洲节点情况 | 入门卡小时价 | 稳定性 | 适合度 |
|------|--------------|--------------|--------|--------|
| **RunPod 社区云** | 法兰克福/阿姆斯特丹节点充足，延迟低 | RTX 3090 约 $0.12–0.18/h | 较好，第三方主机但有平台兜底 | ★★★★★ 学习首选 |
| vast.ai | 德国本土节点少，荷/捷周边节点多 | RTX 3090 低至 $0.06/h | 参差不齐，需手动筛选高可靠主机 | ★★★☆ 补充短任务 |
| Lambda Labs | 主力节点在北美，欧洲资源极少 | A100 80GB 约 $2+/h | 企业级稳定 | ★★☆ 性价比低 |

#### RunPod 优势
- 开箱即用的 PyTorch/Jupyter 模板，VSCode Remote-SSH 无缝适配
- 按秒计费，暂停后仅收存储费
- 欧洲节点延迟普遍在 30ms 以内，旧笔记本 SSH 连接体验流畅
- 分「社区云（低价）」和「安全云（高稳定）」两档，学习用社区云足够

#### vast.ai 注意事项
- 属于 P2P 算力市场，机器质量、网络、环境参差不齐，必须筛选**可靠性 ≥ 99%** 的主机
- 可中断实例会被宿主随时收回，适合短时间实验，不适合跑几小时以上的长训练

#### Lambda Labs 不推荐原因
- 欧洲节点少、德国无直接节点，连接延迟偏高
- 整体价格显著高于前两者，仅做原理学习完全没必要

### 2.3 德国/欧洲补充备选（合规优先）

- **Nebius**：芬兰/巴黎节点，欧盟本土运营，H100 约 $2/小时，合规性强
- **UpCloud**：芬兰节点，L4 24GB 约 €0.58/小时，稳定可靠，适合轻量 RAG、推理部署

---

## 三、200 欧元预算分配方案

### 3.1 核心选型：RunPod + RTX 3090 24GB

- 24GB 显存足以覆盖：小 GPT 预训练、LoRA 微调、nanochat depth=12 模型、7B 模型 4bit 量化推理
- RTX 3090 算力足够支撑学习节奏，比 4090 便宜 30%–40%，性价比最高
- 存储配 50GB 持久化 SSD，存放代码、数据集、小模型权重

### 3.2 详细预算测算（按 12 周满负荷计算）

汇率按 1 欧元 ≈ 1.08 美元估算，取保守高价计算：

| 项目 | 规格 | 单价 | 时长/用量 | 费用（美元） | 约合欧元 |
|------|------|------|-----------|--------------|----------|
| GPU 计算 | RTX 3090 24GB 社区云可中断 | $0.15/h | 12周×7天×2.5h = 210h | 31.5 | ~29 |
| 持久化存储 | 50GB SSD（暂停也保留数据） | $0.1/GB/月 | 3个月 | 15 | ~14 |
| 网络与零星开销 | 下载数据集、节点浮动溢价 | - | - | 10 | ~9 |
| **基础版总计** | - | - | - | **56.5** | **~52** |

即使加上偶尔加练、跑长时训练（累计再增加 100 小时 GPU 时长），总费用也仅约 70–80 欧元，远低于 200 欧元预算。

### 3.3 200 欧元最优分配

| 用途 | 预算 | 说明 |
|------|------|------|
| 核心计算 + 存储 | **80 欧元** | 覆盖 12 周所有 GPU 学习任务，包含偶尔超时跑训练的余量 |
| 预留升级额度 | **70 欧元** | 遇到需要快速跑完的实验，临时升级到 RTX 4090 甚至 A100 40GB；应对节点涨价、临时加练的缓冲 |
| 第三方 API 额度 | **50 欧元** | 分配给 OpenAI/Anthropic 等 API，用于 LLM-as-judge 评测、RAG 效果验证；避免本地跑大评判模型占用显存 |

### 3.4 分阶段成本优化策略

- **阶段1（原理代码）**：大部分代码阅读、单步调试用 CPU 实例即可，仅跑训练循环时开 GPU，可再省约 30% GPU 时长
- **阶段2（nanochat 训练）**：全程 GPU，训练时挂后台跑，不用一直守着，跑完立刻暂停
- **阶段3（工程落地）**：数据处理、RAG 搭建、API 服务基本用 CPU 就能完成，仅模型推理时临时开 GPU

---

## 四、完整学习进度追踪表

> 所有训练/推理任务在远端 GPU 执行；本地旧笔记本仅作为 SSH 客户端操作。

### 阶段1：LLMs-from-scratch 原理与基础实现（4周）

| 阶段 | 周次 | 模块/任务 | 目标产出 | 完成☑ | 备注 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 阶段1 | 第1周 | Ch2 文本数据、BPE分词、数据加载器 | 1.运行章节Notebook<br>2.理解token、上下文窗口、padding<br>3.习题练习<br>✅看懂BPE分词流程，能构造LLM输入数据集 | ☑ 2026-08-15 | 小数据集远端内存充足，基本不会爆显存 |
| 阶段1 | 第1‑2周 | Ch3 多头自注意力 | 1.手写因果多头注意力<br>2.对比普通注意力 vs GPT因果掩码<br>3.习题练习<br>✅清楚因果注意力原理，能独立写出注意力模块 | | |
| 阶段1 | 第2周 | Ch4 完整GPT模型搭建 | 1.完整GPT前向传播<br>2.拆解：嵌入、位置编码、Transformer块、LN、残差连接<br>✅从零搭建可推理的基础GPT模型 | | 控制模型规模，不要直接加载过大 Llama 权重 |
| 阶段1 | 第3周 | Ch5 预训练与文本生成 | 1.训练小参数量GPT<br>2.文本采样生成<br>3.加载轻量Qwen/Llama小版本做推理<br>✅跑通预训练循环，理解训练/推理区别 | | 训练只跑小模型；大权重加载注意显存，优先 7B 以下量化版本 |
| 阶段1 | 第3‑4周 | Ch6 SFT监督微调、文本分类微调<br>Ch7 DPO偏好对齐<br>附录E LoRA | ✅理解SFT、DPO、LoRA；完成对应notebook实验 | | 务必使用 LoRA，不要全参数微调；全参数微调显存压力大 |
| 阶段1 | 第4周 | 复习+验收自测 | 1.复盘2‑7章核心代码<br>2.完成书本习题<br>验收：手写GPT推理代码，讲清预训练/SFT/DPO作用<br>✅阶段1全部完成，底层原理闭环 | | |

### 阶段2：nanochat 端到端对话模型训练（3周）

> 远端拥有 CUDA，可尝试更大模型；但学习目标以看懂代码逻辑为主，不追求训练高质量模型。

| 阶段 | 周次 | 模块/任务 | 目标产出 | 完成☑ | 备注 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 阶段2 | 第5周 | 环境搭建 + 基础预训练 | 1.uv安装依赖<br>2.使用 `--depth 12` 小模型做测试训练，可逐步尝试更大深度<br>3.阅读 `gpt.py`，对比与 rasbt 版本 GPT 实现差异<br>4.搞懂 bpb 损失、梯度累积、bfloat16<br>✅环境跑通，完成小模型预训练实验 | | 远端支持 DDP、fp8；遇到内存上涨调小 `--device-batch-size`；训练任务放 tmux 后台 |
| 阶段2 | 第6周 | SFT、RL对齐、对话推理 | 1.运行 `chat_sft.py` 监督微调<br>2.运行 `chat_rl.py` 强化学习流程<br>3.运行 `chat_cli` 交互式对话<br>对比 nanochat 与 LLMs-from-scratch 的 SFT 实现差异<br>✅完整跑通预训练‑SFT‑RL‑对话流程（小模型） | | RL 训练内存压力大，batch 调至合适大小；以看懂流程为主 |
| 阶段2 | 第7周 | 评测、训练技巧学习 | 1.运行轻量子集评测，不要跑完整 MMLU/GSM8K 全集<br>2.理解 DCLM‑CORE 指标<br>3.阅读 speedrun.sh 看懂高性能训练 trick<br>4.验收：梳理分布式训练、混合精度、MFU 概念<br>✅理解工业训练的工程优化点 | | 完整评测集跑极慢，仅跑样例子集即可；远端可尝试比 Mac 更大的子集 |

### 阶段3：ai-engineering-from-scratch 工业工程落地（3‑5周）

| 阶段 | 周次 | 模块/任务 | 目标产出 | 完成☑ | 备注 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 阶段3 | 第8周 | AI数据工程 | 1.大模型数据集构建、清洗、过滤、去重<br>2.数据集质量对训练的影响<br>✅掌握LLM数据处理完整流程 | | 数据处理对内存友好；避免一次性加载超大 jsonl 文件 |
| 阶段3 | 第8‑9周 | LLM评估体系 | 1.自动评估、LLM-as-judge<br>2.幻觉检测、事实一致性评估<br>✅可以对大模型输出做标准化评估 | | LLM-as-judge 优先调用 OpenAI API，不要本地跑大评判模型，节省显存 |
| 阶段3 | 第9‑10周 | RAG系统完整实现 | 向量库、召回、重排、RAG全链路调试<br>✅实现可用RAG应用 | | 本地向量数据库在远端运行流畅；文档数量不要一次性拉几十万级别 |
| 阶段3 | 第10‑11周 | 模型部署、服务、监控 | 1.模型导出、API服务、流式输出<br>2.实验追踪、日志、生产监控<br>3.生产问题：延迟、吞吐量、幻觉<br>✅把模型封装成可对外服务的系统 | | 远端做演示服务，不做高并发压测；大模型服务建议调用外部 API |
| 阶段3 | 第11周 | 阶段3验收复盘 | 梳理：数据‑训练‑对齐‑评估‑RAG‑部署全链路<br>✅完整掌握LLM工程全栈 | | |

### 拓展选修（主线完成后）

| 拓展 | 选修任务 | 产出 | 完成☑ | 备注 |
| ---- | ---- | ---- | ---- | ---- |
| 拓展1 | LLMs-from-scratch Bonus Material：KV Cache、GQA、MoE、GRPO | 读懂前沿模块实现 | | 只跑代码 demo，不要实例化超大 MoE 模型 |
| 拓展2 | LLaMA-Factory 做大量微调实验 | 快速验证 SFT/DPO/ORPO | | 只做 LoRA/QLoRA 微调，禁止全参数训练 |
| 拓展3 | nanochat 缩放定律实验脚本 | 理解模型缩放规律 | | 仅阅读脚本逻辑，不在本机大规模训练 |

---

## 五、整体验收自测清单

- [ ] 能手写 GPT 核心模块（嵌入、注意力、Transformer 块）
- [ ] 说清预训练 / SFT / DPO / RL 的区别与用途
- [ ] 区分教学向实现(rasbt)与高性能训练实现(nanochat)的代码差异
- [ ] 熟练掌握远程开发：VSCode Remote-SSH、端口转发、tmux 保活、文件同步
- [ ] 清楚云 GPU 成本优化策略，知道什么任务该开 GPU、什么用 CPU 即可
- [ ] 能完成数据集处理、评估、RAG、模型部署的完整流程

---

## 六、RunPod 实操要点

1. **节点优先选法兰克福/阿姆斯特丹**：德国本土延迟最低，SSH 连接稳定
2. **必装 tmux**：训练任务放 tmux 会话里，防止旧笔记本断网导致训练中断
3. **用完手动暂停 Pod**：RunPod 暂停后不收 GPU 费，只收极低存储费，养成随手暂停的习惯
4. **数据集存在持久存储**：不要每次启动重新下载，既浪费时间又产生流量开销
5. **旧本地端只装这三个**：VSCode + Remote-SSH 插件 + Git，所有代码和运行环境全放远端
